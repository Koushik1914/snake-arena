version: '3.8'

services:
  snake-arena:
    build:
      context: .
      dockerfile: Dockerfile
    image: snake-arena:latest
    container_name: snake-arena-game
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
